<Application x:Class="VideoPiper.App"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:conv="using:VideoPiper.Converters"
       xmlns:models="using:VideoPiper.Models">
    <Application.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisible"/>
        <conv:StringToVisibilityConverter x:Key="StringToVisible"/>

        <!-- Library list row: one media item (kind icon, title, meta/status) -->
        <DataTemplate x:Key="LibraryItemTemplate" x:DataType="models:LibraryItem">
            <Border Margin="0,2,0,2" Padding="12,10,12,10" CornerRadius="10"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <FontIcon Grid.Column="0" FontSize="16" VerticalAlignment="Center"
                              Glyph="{Binding IconGlyph}"/>

                    <StackPanel Grid.Column="1" Spacing="2">
                        <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding StatusText}" FontSize="11"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Application.Resources>
</Application>
