import { IndexState } from './indexTypes';

export function video(state: IndexState) {
    return state.video;
}
export function link(state: IndexState) {
    return state.link;
}
export function folder(state: IndexState) {
    return state.folder;
}
